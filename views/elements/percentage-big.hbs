<article class="percentage-big">
    <div>
        <p>{{ title }}</p>
        <h3>{{ percentage }}%</h3>
    </div>
    <p>In vergelijking met vorige week</p>
</article>

<style>
    .percentage-big {
        --line-margin: 5px;
        width: 30vw;
        position: relative;
        background-color: var(--background-highlight);
        padding: 20px 25px;
        font-family: 'Lato', sans-serif;
        border-radius: 10px;
        text-transform: uppercase;
    }

    .percentage-big>div {
        display: flex;
        flex-direction: row;
        justify-content: space-between;
        align-items: flex-end;
        color: var(--khaki);
        border-bottom: 3px solid var(--orange);
        padding-bottom: var(--line-margin);
        width: 100%;
    }

    .percentage-big p {
        margin: 0;
    }

    .percentage-big h3 {
        margin: 0;
        font-size: 2.4em;
        color: var(--khaki);
    }

    .percentage-big>p {
        padding-top: var(--line-margin);
        font-size: 0.8em;
    }

    .percentage-big div p {
        font-size: 1.4em;
    }
</style>